export const ContactsList = contacts => {
  return (
    <ul>
      {/* {contacts.map(contact => (
                <li key={contacts.indexOf(contact)}>
                 {contact.name}  
                </li>
          ))}   */}
    </ul>
  );
};
